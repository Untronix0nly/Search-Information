кривая леви анимация построения кривая леви фрактал предложен французским математиком леви получается если взять половину квадрата вида затем каждую сторону заменить таким же фрагментом повторяя эту операцию пределе получим кривую леви система порождающая кривую леви переменные константы начало правила угол свойства кривая леви нигде не дифференцируема не спрямляема на любом интервале кривой леви есть точки самопересечения хаусдорфова размерность границы кривой леви приблизительно равна кривая леви состоит из двух равных частей каждая из которых подобна всей кривой коэффициентом подобия из за отстуствия существенных самопересечений её размерность точности равна кривая леви крона дерева пифагора вариации стандартная кривая леви строится помощью равнобедренных треугольников углами при основании вариации кривой леви можно построить помощью равнобедренных треугольников другими отличными от углами до тех пор пока угол меньше каждая новая линия короче той линии из которой она образована так что процесс строительства стремится предельной кривой углы менее производят фрактал который менее плотно свернут пример алгоритма на php пример алгоритма на python from turtle import instruction def movement iterations actions none length none if not actions actions if not length length iterations for in actions if if iterations movement iterations instruction length else forward length elif left elif right iterations speed tracer movement iterations update mainloop см также самоподобие дерево пифагора категория фрактальные кривые