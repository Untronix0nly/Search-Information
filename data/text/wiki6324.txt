структура soap сообщения soap от простой протокол доступа объектам протокол обмена сообщениями распределённой вычислительной среде первоначально soap предназначался основном для реализации удалённого вызова процедур rpc сейчас протокол используется для обмена произвольными сообщениями формате xml не только для вызова процедур официальная спецификация последней версии протокола никак не расшифровывает название soap soap является расширением протокола xml rpc soap может использоваться любым протоколом прикладного уровня smtp ftp http https др однако его взаимодействие каждым из этих протоколов имеет свои особенности которые должны быть определены отдельно чаще всего soap используется поверх http soap является одним из стандартов на которых базируются технологии веб служб структура протокола сообщение soap выглядит так soap конверт пример пример soap запроса на сервер интернет магазина пример ответа стакан граненый стакан граненый мл usd us dollar true недостатки использование soap для передачи сообщений увеличивает их объём снижает скорость обработки системах где скорость важна чаще используется пересылка xml документов через http напрямую где параметры запроса передаются как обычные http параметры см также rest wsdl wddx ссылки спецификация soap soap версия часть учебник для начинающих категория сетевые протоколы категория веб категория xml категория стандарты интернета категория стандарты на основе xml категория стандарты