фу нкция фрагмент программного кода подпрограмма которому можно обратиться из другого места программы большинстве случаев функцией но многие языки допускают безымянные функции именем функции неразрывно связан адрес первой инструкции оператора входящей функцию которой передаётся управление при обращении функции после выполнения функции управление возвращается обратно адрес возврата точку программы где данная функция была вызвана функция может принимать параметры должна возвращать некоторое значение возможно пустое функции которые возвращают пустое значение часто называют процедурами некоторых языках объявления функций процедур имеют различный синтаксис частности могут использоваться различные ключевые слова функция должна быть соответствующим образом объявлена определена объявление функции кроме имени содержит список имён типов передаваемых параметров или аргументов также тип возвращаемого функцией значения определение функции содержит исполняемый код функции одних языках объявление функции непосредственно предваряет определение функции то время как ряде других языков необходимо сначала объявить функцию уже потом привести её определение объектно ориентированном функции объявления которых являются неотъемлемой частью определения класса называются методами для того чтобы использовать ранее определённую функцию необходимо требуемом месте программного кода указать имя функции перечислить передаваемые функцию параметры параметры которые передаются функции могут передаваться как по значению так по ссылке для переменной переданной по значению создаётся локальная копия любые изменения которые происходят теле функции переданной переменной на самом деле происходят локальной копией никак не сказываются на самой переменной то время как изменения которые происходят теле функции переменной переданной по ссылке происходят самой переданной переменной функция определяет собственную локальную область видимости куда входят входные параметры также те переменные которые объявляются непосредственно теле самой функции существует возможность вызвать функцию внутри самой функции такой вызов функции называется рекурсивным сам процесс вложенных друг друга вызовов функций называют рекурсией поскольку необходимо запомнить стеке адрес возврата функции также выделить том же стеке память под параметры локальные переменные не являющиеся динамическими то ничем неограниченная рекурсия приводит переполнению стека поэтому языках устанавливается некоторый предельный уровень вложенности рекурсивных вызовов примеры функций javascript function max return actionscript public function name text string var textfield textfield new textfield textfield text text void name string text cout стандарте существуют так же функции аргументами по умолчанию void foo int arg int arg cout public void name string text system console writeline text java public void name string text system out println text pascal procedure name var text string begin write text end php function name text echo text standard ml fun name print или что то же самое см функции первого класса лямбда функция val name fn print visual basic sub name text console writeline text end sub purebasic procedure name text printn text endprocedure python def func text print text см также анонимная функция функция математика ссылки категория концепции языков