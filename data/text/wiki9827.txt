метка символьное имя идентификатор для более удобного указания данных кода языках позволяет программисту обходиться без вычисления пересчёта адресов смещений внутри программы эти действия за него выполняет компилятор несмотря на то что большинство языков высокого уровня поддерживают операции метками их использование крайне нежелательно так как программный код становится плохочитаемым отладка такой программы занимает большее время при компиляции каждой метке сопоставляется определённый адрес число который заносится исполняемый модуль таким образом случае необходимости компилятор может вычислить смещение между метками разность адресов большинстве языков формат записи метки такой название_метки сначала идет название метки также записанное по определенным правилам зависимости от языка затем двоеточие затем код или данные доступ которым будет осуществляться по названию метки пример mov eax offset data jmp label_ label_ label_ data db привет во время ассемблирования метке data будет сопоставлено число адрес первого байта строки привет вместо offset data первой строке будет записано это число во второй строке примера осуществляется безусловный переход jump на инструкции следующие после метки label_ если адрес абсолютный аргументом команды перехода является сам адрес то во второй строке вместо label_ будет записан адрес метки label_ если же переход относительный задается смещением относительно текущей метки тогда label_ этой же второй строке будет заменён на разность адресов label_ label_ смещением между метками см также компилятор язык goto категория концепции языков